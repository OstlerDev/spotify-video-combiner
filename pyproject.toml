[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "spotify-video-combiner"
version = "0.1.0"
description = "Download a Spotify playlist's audio + cover art and combine it into a single MP4 (built for VRChat-via-YouTube use)."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [{ name = "Sky" }]
keywords = ["spotify", "playlist", "video", "ffmpeg", "vrchat", "zotify"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Operating System :: OS Independent",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Multimedia :: Video :: Conversion",
]
dependencies = [
    "click>=8.1",
    "Pillow>=12.0",
    # music-tag is the cross-format tag reader (mp3/ogg/m4a/flac/...) we use
    # to pull title/artist/album/cover-art back out of zotify's downloads.
    # It is already a transitive dep via zotify but we depend on it directly
    # because we read tags ourselves.
    "music-tag>=0.4",
    # Zotify (DraftKinner's actively-maintained fork) is our one-stop downloader:
    # it handles sign-in (via `zotify.OAuth`), playlist scanning, audio download,
    # and embeds title/artist/album/cover-art into each output file. We reuse
    # its `OAuth` + `Session` classes for in-app sign-in and shell out to its
    # CLI for the actual download. Pinned to the git URL because it is not on
    # PyPI; pip will build a wheel locally on install.
    "zotify @ git+https://github.com/DraftKinner/zotify.git",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
]
build = [
    "pyinstaller>=6.10",
]

[project.scripts]
svc = "spotify_video_combiner.cli:main"
spotify-video-combiner = "spotify_video_combiner.cli:main"
svc-gui = "spotify_video_combiner.gui:main"

[project.urls]
Homepage = "https://github.com/Skyrior/spotify-video-combiner"
Issues = "https://github.com/Skyrior/spotify-video-combiner/issues"

[tool.hatch.metadata]
# Required because we depend on zotify via a git URL (it is not on PyPI).
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/spotify_video_combiner"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501"]
